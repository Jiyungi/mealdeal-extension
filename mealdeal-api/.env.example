# Server-only. Never expose to the extension.
APIFY_TOKEN=
APIFY_ACTOR_ID=king_carl~mealdeal-scraper

# Optional: polling window (ms) used by /api/run-mealdeal when waiting inline
MEALDEAL_MAX_WAIT_MS=60000
