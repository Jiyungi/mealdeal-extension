{
  "name": "mealdeal-api",
  "version": "0.1.0",
  "private": true,
  "description": "Thin backend bridge that starts the MealDeal Apify Actor and returns MealDealResult to the Chrome extension. Holds APIFY_TOKEN server-side only.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "apify-client": "^2.12.0",
    "next": "^15.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.12",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
